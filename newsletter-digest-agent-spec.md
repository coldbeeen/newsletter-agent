# 요구사항 명세서: 네이버 메일 뉴스레터 데일리 요약 에이전트

> 이 문서는 초안입니다. 구현 시작 전, 이 스펙을 읽고 모호하거나 결정되지 않은 부분(`[결정 필요]` 표시)을 나에게 질문하여 스펙을 완성한 뒤, 완성본을 기준으로 구현 계획을 제시하고 승인받으세요.

## 1. 목적과 배경 (Why)

네이버 메일함에 기술 트렌드 뉴스레터가 하루에도 여러 개씩 쌓이지만, 뉴스레터 본문과 본문 내 링크된 원문까지 일일이 확인할 시간이 없다. 매일 쌓인 뉴스레터의 핵심 내용을 하나의 데일리 리포트로 요약해 받아봄으로써, 확인 시간을 하루 수십 분 → 5분 이내로 줄이는 것이 목표다.

## 2. 에이전트의 역할 정의 (What)

- **입력**: 네이버 메일함에서 최근 24시간 내 수신된 기술 뉴스레터 이메일
  - 예: 요즘IT, GeekNews Weekly, TLDR, Substack 기반 기술 뉴스레터 등
- **출력**: 하나의 데일리 리포트 (마크다운 형식)
  - 구성: ① 오늘의 핵심 요약 3~5줄 → ② 주제별(예: AI, 개발, 스타트업) 그룹핑된 기사 요약 → ③ 각 항목에 원문 링크 첨부
  - 언어: 한국어 (원문이 영어여도 한국어로 요약)
- **트리거**: 매일 1회 스케줄 실행. 실행 시각: 매일 오전 8시 (KST)
- **"최근 24시간" 판단 기준**: IMAP `SINCE` 검색으로 어제 날짜(KST 기준)에 수신된 메일을 가져온 뒤, 화이트리스트(`whitelist.yaml`) 발신자에 해당하는 메일만 대상으로 필터링

## 3. 범위 (Scope)

**In scope**

- 지정된 발신자(화이트리스트)의 미읽음/최근 메일 수집
- 메일 본문(HTML) 파싱 및 본문 내 기사 링크 추출
- 각 링크의 원문 페이지 fetch 및 본문 텍스트 추출
- LLM(Claude API)을 이용한 개별 기사 요약 + 전체 리포트 합성
- 완성된 리포트 전달. 전달 방식: **Slack 웹훅**
  - 사유: 네이버 메일함(뉴스레터 원본 수신처)과 리포트 확인 매체를 분리하기 위함. 모바일 Slack 앱 푸시 알림으로 확인 가능하며, 카카오톡 대비 토큰 갱신 부담이 없음
  - Slack Incoming Webhook URL은 자격 증명과 동일하게 `.env`/환경변수로 관리 (`.gitignore` 처리)

**Out of scope (하지 말 것)**

- 메일 삭제, 이동, 읽음 처리 등 메일함 상태 변경 일체
- 뉴스레터 구독/해지 등 외부 서비스에 대한 쓰기 작업
- 화이트리스트 외 개인 메일 접근 및 처리
- 웹 UI, 대시보드 등 리포트 열람 외의 부가 기능

## 4. 기술 제약사항

- **메일 접근**: IMAP 프로토콜 사용 (네이버 메일: `imap.naver.com:993`, SSL)
  - 사전 조건: 네이버 메일 설정에서 IMAP/SMTP 사용 활성화 필요. 2단계 인증 사용 시 애플리케이션 비밀번호 발급 필요 → 정확한 설정 방법은 구현 시 네이버 공식 도움말에서 확인할 것
  - 자격 증명은 코드에 하드코딩 금지. 환경변수 또는 `.env` 파일 사용, `.gitignore` 처리
- **언어/런타임**: Python 3.11+ (imaplib/imap-tools, BeautifulSoup, httpx 등 생태계 성숙)
- **요약 모델**: Anthropic Claude API 사용. 비용 효율 우선 — 기본값은 Claude Haiku 4.5. 요약 품질이 부족하면 Sonnet 계열로 전환 검토. 매일 08:00 KST 도착이 고정 요구사항이므로 Batch API(비동기, 지연 발생)는 사용하지 않고 실시간 API 사용
  - 참고: 기사 100개/일 기준 예상 비용은 Haiku 4.5 약 $0.35/일(~$10.5/월), Sonnet 5 약 $0.70/일(~$21/월) 수준 (2026-07 기준 표준 API 가격, 실제 토큰 수에 따라 변동)
- **전달 채널**: Slack Incoming Webhook (HTTPS POST)
- **스케줄링**: GitHub Actions (scheduled workflow, cron 트리거)
  - 자격 증명(네이버 IMAP 계정/앱 비밀번호, Anthropic API 키, Slack Webhook URL)은 GitHub Actions Secrets에 저장
  - 저장소 공개 범위: **Private**
  - GitHub Actions cron은 UTC 기준. 매일 오전 8시(KST) = 전날 23:00 UTC → `cron: '0 23 * * *'`
- 프로젝트 공통 컨벤션은 CLAUDE.md에 별도 정리 (이 스펙에는 미포함)
- **IMAP 읽음 플래그**: 별도 처리(PEEK) 불필요. 에이전트가 조회한 메일에 `\Seen` 플래그가 붙는 것을 허용 (에이전트가 실제로 어떤 메일을 읽었는지 확인하기 위함)
- **실행 간 상태 저장**: 중복 제거 판단을 위해 최근 처리한 기사 URL 목록을 영속 저장해야 함. 저장 방식: 저장소 내 상태 파일(예: `state/seen_urls.json`)을 매 실행 종료 시 GitHub Actions가 커밋. 상태 파일에는 처리한 기사 URL과 처리 일자를 기록하고, 오래된 항목은 적절한 보존 기간(예: 최근 N일) 이후 정리
- **실행 로그**: GitHub Actions 콘솔 로그(기본 90일 보관)로 충분. 별도 저장소/전송은 현재 범위에서 제외, 추후 필요 시 추가 검토

## 4-1. 화이트리스트 (발신자 목록)

- 관리 방식: 별도 설정 파일 `whitelist.yaml` (코드 수정 없이 추가/삭제 가능, 저장소에 커밋)
- **1차 판별 기준**: 발신 이메일 주소 (아래 목록에 포함된 주소의 메일만 수집 대상)
- **2차 구분 기준**: 동일 발신 주소를 여러 뉴스레터가 공유하는 경우(TLDR 계열), 메일 헤더의 `From` 표시 이름(display name)으로 하위 종류를 구분
  - 예: `"TLDR AI" <dan@tldrnewsletter.com>` → 표시 이름 "TLDR AI"로 식별
  - 표시 이름 패턴이 예상과 다르거나 파싱 실패 시: 오류로 중단하지 않고 "TLDR(미분류)"로 폴백 처리, 실행 로그에 기록 (사일런트 실패 방지)
  - 실제 발행 메일의 `From` 헤더 형식은 구현 착수 시 샘플 메일로 검증 필요
- **기사 링크 판별 기준**: 발신자별(TLDR/GeekNews Weekly/routinecrew) 최소 규칙 + 공통 필터를 혼합 적용
  - 화이트리스트 발신자가 고정되어 있으므로, 발신자별로 "본문 영역 범위(footer 이전만 대상)" 같은 가벼운 규칙을 1~2개씩 둠
  - 공통 필터로 구독해지/SNS 공유/추적 픽셀 등 URL 패턴(unsubscribe, twitter.com, share 등 키워드)을 제외
  - 완전한 발신자별 전용 파서보다 구현 부담이 적고, 범용 휴리스틱만 쓰는 것보다 정확도가 높음

| 뉴스레터        | 발신 이메일 주소                |
| --------------- | ------------------------------- |
| TLDR IT         | dan@tldrnewsletter.com          |
| TLDR AI         | dan@tldrnewsletter.com          |
| TLDR Marketing  | dan@tldrnewsletter.com          |
| TLDR Data       | dan@tldrnewsletter.com          |
| TLDR            | dan@tldrnewsletter.com          |
| GeekNews Weekly | news@hada.io                    |
| Newsletter      | newsletter@mail.routinecrew.net |

## 5. 에이전트 동작 명세

**정상 흐름**

1. IMAP으로 접속해 화이트리스트 발신자의 최근 24시간 메일을 조회 (삭제/이동 없는 읽기 전용 조회. 읽음 플래그 변경은 허용)
2. 각 메일의 HTML 본문에서 기사 링크 추출 (추적용 리다이렉트 URL은 최종 목적지 URL로 해석)
3. 링크별로 원문 fetch → 본문 텍스트 추출 → 개별 요약 (기사당 3~5문장)
4. 개별 요약들을 주제별로 그룹핑하고 전체 핵심 요약을 생성해 하나의 리포트로 합성
5. 지정된 방식으로 리포트 전달 후 실행 로그 기록

**예외 / 엣지케이스**

- 링크 fetch 실패: 원인별로 구분하여 처리하고 리포트 표시 문구를 다르게 함. 두 경우 모두 뉴스레터 본문에 포함된 발췌문만으로 요약
  - 404/타임아웃/페이월/로그인 필요 → 리포트에 "원문 확인 불가" 표시
  - 봇 차단(403, Cloudflare 등 크롤링 차단) → 리포트에 "접근 차단됨" 등으로 구분 표시
  - 두 경우 모두 실행 로그에 원인(상태 코드/사유)을 구분하여 기록 (추후 특정 사이트가 자주 막히는지 분석 가능하도록)
- 동일 기사가 여러 뉴스레터에 중복 등장: URL 정규화 기준으로 중복 제거하고 1회만 요약, 출처 뉴스레터는 병기
- 어제 이미 리포트에 포함된 기사가 오늘도 뉴스레터에 등장: 상태 파일(`state/seen_urls.json`) 기준으로 중복 제외 (오늘 리포트에 포함하지 않음)
- 해당일 수신 뉴스레터가 0건: "오늘 수신된 뉴스레터 없음" 리포트 발송 (무소식도 알림)
- IMAP 인증 실패: 재시도 없이 즉시 중단하고 오류 알림 (계정 잠금 방지)
- 링크가 과도하게 많은 경우(예: 50개 초과): 뉴스레터 내 상위 노출 순으로 상한 개수까지만 처리하고 리포트에 명시
- API 호출 실패: 지수 백오프로 최대 3회 재시도, 최종 실패 시 부분 리포트라도 발송
- Slack Webhook 전송 실패: API 호출과 동일하게 지수 백오프 최대 3회 재시도, 최종 실패 시 GitHub Actions 실행을 실패(fail) 상태로 남김

**자율성 경계**

- 에이전트 스스로 결정 가능: 요약 분량 미세 조정, 주제 그룹핑 기준, 처리 순서
- 반드시 사람 확인 필요: 화이트리스트 변경, 전달 채널 변경, 메일함에 대한 읽기 외 작업 (원칙적으로 금지)

## 6. 검증 기준 (Definition of Done)

- 단위 테스트: 링크 추출, 리다이렉트 해석, 중복 제거, 본문 추출 로직 각각에 대해 통과
- 통합 테스트: 샘플 뉴스레터 HTML 3종(fixture)을 입력하면 기대 형식의 리포트가 생성됨
- 예외 테스트: fetch 실패·0건 수신·중복 기사 시나리오에서 위 명세대로 동작
- 실제 계정 대상 E2E 1회: GitHub Actions `workflow_dispatch`로 수동 트리거하여 실제 운영 환경(Actions 러너 + Secrets)에서 실행, 메일 삭제/이동 등 상태 변경이 없음을 확인 (읽음 플래그는 변경 허용, [4장](#4-기술-제약사항) 참고)
  - 이후 정기 실행은 `schedule` cron 트리거로 자동화되어 별도 개입 불필요
- 전체 실행 시간 10분 이내, 자격 증명이 로그·리포트에 노출되지 않음
- README에 설치, 환경변수, 스케줄 등록 방법 문서화

## 7. 구현 계획 요청

- 구현 전 phase별 계획을 먼저 제시하고 승인받을 것
- 각 phase는 "수집 → 요약 → 전달"의 전 구간을 관통하는 수직 슬라이스로 구성할 것
  - 예: Phase 1은 메일 1통 → 링크 없이 본문만 요약 → 콘솔 출력까지 end-to-end로 동작
  - 이후 phase에서 링크 추적, 중복 제거, 전달 채널, 스케줄링을 점진 추가
- 각 phase 완료 조건에 해당 phase의 테스트 통과를 포함할 것
